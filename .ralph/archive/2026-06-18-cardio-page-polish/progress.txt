# Ralph Progress Log — cardio-page-polish
Started: 2026-06-17

## Codebase Patterns
- **Gradle env**: JAVA_HOME=/Library/Java/JavaVirtualMachines/jdk-21.jdk/Contents/Home; run UNSANDBOXED. Typecheck = `:composeApp:compileDebugKotlinAndroid` + `:composeApp:compileKotlinIosSimulatorArm64`. No browser test.
- **Header pattern**: `MacroProfilesSettings.kt` lines 83–99 — `Column(Modifier.background(surface).statusBarsPadding().padding(horizontal=24.dp))` with title in `headlineMedium`/`displayMedium` and subtitle in `labelSmall` + `MiOrange`. No TopAppBar — plain Column.
- **CardioScreen**: `ui/dashboard/CardioScreen.kt` — wraps content in `Scaffold` with MiOrange FAB. Header goes in a `Column` above the `Scaffold`, pinned by putting both in an outer `Column` (header + Scaffold with `Modifier.weight(1f)`).
- **DashboardContent**: `DashboardScreen.kt` — `DashboardContent(state, onRefresh, onDateRangeChanged, viewMode...)`. SlidingWindowInsightCard rendered ~line 247. History section header ~line 256. CardioSummaryRow goes between them.
- **DashboardViewMode**: `DashboardViewMode.kt` — `BALANCE`, `INTAKE`, `EXPENDITURE`. Check `viewMode == DashboardViewMode.BALANCE` to show/hide row.
- **Icons**: `Icons.AutoMirrored.Filled.DirectionsRun` (not `Icons.Default` — deprecated).
- **Colors**: `MiOrange` = 0xFFFF6B00, `MiTextSecondary` = 0xFF6C757D (both in `ui/Theme.kt`).
- **CardioStateHolder**: already instantiated in `App.kt` via `remember {}`. `state: StateFlow<CardioState>`. `CardioState.weeklyTotalMinutes: Int`.
- **App.kt pattern**: collect StateFlow with `cardioStateHolder.state.collectAsState().value` inside the composable scope.
- **DashboardWithTabs collect pattern**: collect child StateHolder state inside `AnimatedContent` block — `val x by stateHolder.state.collectAsState()` — then pass value down to `DashboardScreen`. Works because `AnimatedContent` lambda is `@Composable`.

---

## 2026-06-18 — US-002: Weekly cardio total on main dashboard

**What changed:**
- `DashboardScreen.kt`: added `weeklyCardioMinutes: Int = 0` to both `DashboardScreen` and `DashboardContent`; new private `CardioSummaryRow` composable (run icon + label + right-aligned minutes); rendered between `SlidingWindowInsightCard` and History header, guarded by `viewMode == BALANCE`; added `Icons.AutoMirrored.Filled.DirectionsRun` import.
- `DashboardWithTabs.kt`: collect `cardioStateHolder.state.collectAsState()` inside `AnimatedContent` lambda; pass `weeklyTotalMinutes` to `DashboardScreen`.

**Files touched:** `DashboardScreen.kt`, `DashboardWithTabs.kt`

**Learnings:**
- `AnimatedContent` lambda is `@Composable` — safe to call `collectAsState()` inside it.
- Adding a default-value param (`= 0`) to `DashboardContent` keeps all existing call sites compile-clean with no changes.
