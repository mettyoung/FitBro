# Ralph Progress Log — nav-cardio-daterange
Started: 2026-06-18

## Codebase Patterns
- **Gradle env**: JAVA_HOME=/Library/Java/JavaVirtualMachines/jdk-21.jdk/Contents/Home; run UNSANDBOXED. Typecheck = `:composeApp:compileDebugKotlinAndroid` + `:composeApp:compileKotlinIosSimulatorArm64`. No browser test.
- **DashboardWithTabs.kt**: `ui/dashboard/DashboardWithTabs.kt` — tab nav wiring. Current order 0=Balance, 1=Macros, 2=Cardio, 3=Settings. Has `when(targetIndex)` block. Passes `cardioState.weeklyTotalMinutes` to DashboardScreen as `weeklyCardioMinutes`.
- **CardioStateHolder.kt**: `ui/dashboard/CardioStateHolder.kt` — currently hardcodes `startDate = today.minusDays(6)` and `val state = repository.sessionsForRange(startDate, today).map { sessions -> CardioState(sessions, weeklyTotalMinutes = sessions.sumOf { it.minutes }) }`. `DateRange` is already used by `DashboardStateHolder` so it's available.
- **CardioState**: data class with `sessions` and `weeklyTotalMinutes: Int` field. Lives at top of `CardioStateHolder.kt`.
- **DateRange**: `data/model/DateRange.kt` — data class with `startDate: String` and `endDate: String`. `todayString()` util available; `minusDays()` extension on String (YYYY-MM-DD).
- **DashboardStateHolder.kt**: exposes `state: StateFlow<DashboardState>` where `DashboardState` has `selectedDateRange: DateRange`. This is what DashboardWithTabs observes.
- **DashboardScreen.kt**: `DashboardContent` composable takes `weeklyCardioMinutes: Int`. Banner `extraContent` lambda: `Text("🏃 $weeklyCardioMinutes min this week", ...)`. Just drop "this week" suffix.
- **flatMapLatest**: requires `import kotlinx.coroutines.flow.flatMapLatest`. `MutableStateFlow` from `kotlinx.coroutines.flow`.
- **Colors**: `MiOrange`, `MiTextSecondary` in `ui/FitBroTheme.kt`.

---

## 2026-06-18 — US-001, US-002, US-003

**US-001: Reorder bottom nav tabs**
Files: `ui/dashboard/DashboardWithTabs.kt`
- Swapped Macros (was index 1) and Cardio (was index 2) in `items` list
- Updated `when(targetIndex)` dispatch: 1→CardioScreen, 2→MacroDailyCounterDetail

**US-002: CardioStateHolder reactive to dashboard date range**
Files: `ui/dashboard/CardioStateHolder.kt`, `CardioState.kt`, `CardioScreen.kt`, `DashboardWithTabs.kt`
- `CardioStateHolder`: replaced hardcoded date vars with `_dateRange: MutableStateFlow<DateRange>`; derived `state` via `flatMapLatest`; added `setDateRange()`
- `CardioState.weeklyTotalMinutes` → `totalMinutes` (all references updated)
- `DashboardWithTabs`: added `LaunchedEffect(dashboardState.selectedDateRange)` → `cardioStateHolder.setDateRange(it)`

**US-003: Balance banner label**
Files: `ui/dashboard/DashboardScreen.kt`
- Banner text: `"🏃 $weeklyCardioMinutes min this week"` → `"🏃 $weeklyCardioMinutes min"`

Learnings:
- `DateRange` lives in `ui/dashboard/` package (not `data/model/`); CardioStateHolder already in same package so no import needed
- `stateHolder.state.collectAsState()` + keyed `LaunchedEffect` is cleaner than `LaunchedEffect(Unit) { flow.collect { } }` for simple one-way sync
- `flatMapLatest` requires import `kotlinx.coroutines.flow.flatMapLatest`; `MutableStateFlow` from same package

---

