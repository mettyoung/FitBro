# Ralph Progress Log — food-diary-custom-meals
Started: 2026-06-06
---

## Codebase Patterns (read FIRST, carried forward from macro-food-diary run)
- Gradle typecheck: ./gradlew :composeApp:compileDebugKotlinAndroid (Android) + ./gradlew :composeApp:compileKotlinIosSimulatorArm64 (iOS). BOTH must pass before commit.
- SQLDelight 2.x KMP, db package com.mettyoung.fitbro.data.db, .sq files in src/commonMain/sqldelight/com/mettyoung/fitbro/data/db/, migrations as N.sqm in .../db/migrations/
- SQLDelight transactionWithResult {} to return inserted ids; lastInsertRowid() query already exists in FoodDiary.sq
- Dispatchers.IO NOT available in K/N commonMain — use Dispatchers.Default for SQLDelight mapToList/mapToOne/withContext
- mapToOne on COALESCE aggregates -> nullable Double, guard with ?: 0.0
- String.format() NOT available in KMP commonMain — manual arithmetic; no kotlinx-datetime, use util/DateUtil (todayString, plusDays, minusDays)
- State holders in com.mettyoung.fitbro.ui.{feature}, take CoroutineScope constructor param; UDF: expose StateFlow<UiState>, stateIn(scope, WhileSubscribed(5000), initial)
- App.kt wires data sources + repositories + state holders via remember { ... }; platform factories (createSqlDriver, createHealthDataSource) called here
- FoodDiaryEntry stores ABSOLUTE macros for its servingSizeG; gram-mode edit (FoodLogSheet FoodEntryContent) rescales by density = macro/servingSizeG via gramModeData — keep servingSizeG populated on flattened entries
- Diary list + meal sections render in MacroDailyCounterDetail.kt (FoodDiarySection); entries grouped via FoodDiaryStateHolder.state.entriesByMeal (Map<mealType, List<FoodDiaryEntry>>)
- HealthConnect write is fire-and-forget no-op on Android alpha12 + iOS; addEntry already triggers syncToHealthConnect
- iOS cinterop: use `is CPointer<*>` not erased typed refs; kotlinx-coroutines-core explicit in commonMain deps
- Always `git status` before creating files — scaffolding may exist untracked
- Gradle: export JAVA_HOME=/Library/Java/JavaVirtualMachines/jdk-21.jdk/Contents/Home AND run with sandbox disabled (~/.gradle/wrapper outside sandbox write scope)
- Data-layer tests: no SQLDelight test driver in catalog (only android/native drivers, can't add) + no kotlinx-coroutines-test. Test repo CONTRACT via in-memory fake implementing the repo interface; run suspend bodies with a synchronous startCoroutine(Continuation(EmptyCoroutineContext){}) helper since fakes don't truly suspend. See FoodDiaryReorderTest.kt.
- COALESCE(MAX(col), -1) aggregate => SQLDelight NON-null Long; bare MAX/SUM => nullable (guard with ?: )
- COALESCE(MAX(col), -1) query => SQLDelight returns NON-null Long; executeAsOne() directly. Bare MAX/SUM/AVG aggregates stay nullable (guard ?: 0.0). Same COALESCE(...,0.0) -> non-null Double.
- Gradle build env: export JAVA_HOME=/Library/Java/JavaVirtualMachines/jdk-21.jdk/Contents/Home. ~/.gradle/wrapper is OUTSIDE sandbox write scope -> run gradle with sandbox DISABLED.
- NO SQLDelight test driver in version catalog (only android-driver + native-driver, no sqlite/jdbc). Cannot write DB-integration tests without a forbidden new dep -> data-layer SQL-wiring stories rely on typecheck gates only.
- sqldelight migrations auto-detected (schemaVersion not pinned in build.gradle.kts); just add N.sqm under db/migrations/, no build.gradle change.
---

## 2026-06-06 — US-001 Persisted sort order for food diary entries (DONE)
Changes:
- FoodDiary.sq: added sortOrder column to CREATE TABLE + insertEntry; getEntriesByDate / getEntriesByDateAndMeal ORDER BY 'mealType, sortOrder ASC, id ASC'; new queries maxSortOrderForDateMeal (COALESCE(MAX,-1)) and updateSortOrder.
- migrations/2.sqm: ALTER ADD COLUMN sortOrder + backfill via correlated subquery (COUNT earlier ids in same date+mealType).
- FoodDiaryEntry.kt domain model: sortOrder: Long = 0 (already present).
- FoodDiaryRepository.kt: reorderMeal(date, mealType, orderedIds) signature.
- FoodDiaryRepositoryImpl.kt: addEntry computes nextSortOrder = maxSortOrderForDateMeal+1 inside existing transactionWithResult; reorderMeal updates sortOrder=index per id in one transaction on Dispatchers.Default; toDomain maps sortOrder.
Gates: both typecheck commands BUILD SUCCESSFUL (only pre-existing elvis/icon warnings).
Data-layer test: FoodDiaryReorderTest.kt (commonTest, 2 tests, PASS) locks the sortOrder contract via an in-memory FoodDiaryRepository fake (addEntry -> max+1 per date+meal; reorderMeal -> sortOrder=index; totals unchanged by reorder). Real SQL not exercised because no SQLDelight test driver exists in the catalog and adding sqlite/jdbc-driver would violate the no-new-deps rule; the fake mirrors the impl's ORDER BY + transaction semantics.
Learnings:
- COALESCE(MAX(col), -1) query => SQLDelight generates NON-null Long return; executeAsOne()+1 directly, no ?: guard needed (contrast: bare MAX/SUM aggregates are nullable).
- No DB integration test harness exists; no sqldelight sqlite/jdbc driver in gradle/libs.versions.toml (only android-driver + native-driver). For data-layer stories, test the repository CONTRACT via an in-memory fake implementing the repo interface (StateFlow-backed) instead of real SQL — keeps to no-new-deps rule.
- commonTest has NO kotlinx-coroutines-test (no runTest) and no common runBlocking. Run suspend tests synchronously via a tiny helper: `block.startCoroutine(Continuation(EmptyCoroutineContext){...})` — works when the fake never truly suspends (StateFlow.first() emits immediately).
- Gradle needs JAVA_HOME=/Library/Java/JavaVirtualMachines/jdk-21.jdk/Contents/Home; gradle wrapper dir (~/.gradle/wrapper) is OUTSIDE sandbox write scope -> run gradle with sandbox disabled.
- sqldelight migration auto-detected (schemaVersion not pinned in build.gradle.kts); just drop N.sqm, no build.gradle change needed.

## 2026-06-06 — US-002 Drag-to-reorder entries within a meal section (DONE)
Changes:
- FoodDiaryStateHolder.kt: reorderMeal(date, mealType, orderedIds): Job delegating to repository.reorderMeal (reuses US-001 data layer).
- MacroDailyCounterDetail.kt: new ReorderableEntries composable replaces the plain forEachIndexed render inside FoodDiarySection. Manual drag via detectDragGesturesAfterLongPress on a Menu-icon drag handle; local `items` state seeded from upstream `entries` (remember(entries) so StateFlow stays source of truth after refresh). Tracks per-row heights via onGloballyPositioned, swaps order when dragOffsetY crosses neighbor midpoint, graphicsLayer translationY + zIndex for lift. onDragEnd persists items.map{id} via onReorder.
- FoodDiarySection gained onReorder param; call site wires onReorder -> foodDiaryStateHolder.reorderMeal(selectedDate, mealType, orderedIds).
- FoodEntryRow gained optional dragHandle: (@Composable ()->Unit)? slot rendered leading the row.
Gates: both typecheck commands BUILD SUCCESSFUL (only pre-existing elvis/icon warnings). No data-layer change -> no new test (reorderMeal contract already locked by FoodDiaryReorderTest from US-001).
Learnings:
- Drag-reorder confined to a section by rendering a plain Column inside the section card (already nested in the outer LazyColumn); no cross-section moves possible since each section owns its own ReorderableEntries + items list.
- Seed local reorder state with `remember(entries) { mutableStateOf(entries) }` so an upstream StateFlow refresh resets the optimistic order — keeps persisted DB order authoritative without flicker.
- Midpoint-crossing swap (compare accumulated dragOffsetY against neighbor height/2, then subtract that height from offset) gives stable reordering without a third-party reorderable lib.

## 2026-06-06 — US-003 Custom meal persistence layer (DONE, finished by main session after loop hit account session limit)
Changes:
- CustomMeal.sq: CustomMeal + CustomMealItem tables (+ index on customMealId); queries insertCustomMeal, lastInsertRowId, insertCustomMealItem, getAllCustomMeals (name ASC), getItemsForMeal (sortOrder ASC), updateCustomMealName, deleteCustomMeal, deleteItemsForMeal.
- CustomMeal.kt model: CustomMeal(id,name,createdAt,items) + CustomMealItem(... foodId, sortOrder). Absolute macros for servingSizeG (density rescale convention).
- CustomMealRepository + Impl: getAllCustomMeals Flow (asFlow+mapToList, nested executeAsList for items per meal); createCustomMeal transactionWithResult (insert meal + items with sortOrder=index, createdAt=todayString()); renameCustomMeal; deleteCustomMeal transaction (items then meal). Main-safe Dispatchers.Default.
- App.kt: customMealRepository = remember(database) { CustomMealRepositoryImpl(database) }.
Gates: Android + iOS compile BUILD SUCCESSFUL (only pre-existing elvis/icon warnings).
Learnings:
- customMealQueries.customMealQueries auto-generated; SQLDelight FK needs no extra config for compile.
- getAllCustomMeals re-emits on CustomMeal table change; item-only edits won't retrigger (acceptable — current API creates/deletes whole meals only).
- customMealRepository currently unused until US-004 (state holder) -> harmless unused-val warning.

## 2026-06-06 — US-004 Custom meal CRUD interface (DONE, main session)
Changes:
- CustomMealStateHolder.kt: customMeals StateFlow (repo.getAllCustomMeals stateIn WhileSubscribed); create/rename/delete -> Job.
- CustomMealManagerSheet.kt: ModalBottomSheet, two modes — list (name + item count + total kcal, empty state, rename via AlertDialog, delete via confirm AlertDialog, "New custom meal" button) and builder (name field + items list w/ per-item remove + "Add food" that reuses FoodSearchSheet). FoodDiaryEntry.toCustomMealItem(sortOrder) maps search result -> item.
- Threaded customMealStateHolder: App.kt (remember) -> DashboardWithTabs param -> MacroDailyCounterDetail param. Entry point: AutoMirrored List icon in header next to Settings opens the sheet.
Gates: Android + iOS BUILD SUCCESSFUL.
Learnings:
- Reuse FoodSearchSheet (a ModalBottomSheet) nested inside CustomMealManagerSheet's builder by toggling a `searching` flag — stacking ModalBottomSheets works; its onAddEntry yields a FoodDiaryEntry which maps cleanly to CustomMealItem (same macro fields).
- New StateHolder constructor (repo, scope) wired in App.kt with remember(customMealRepository, scope); pass through DashboardWithTabs.

## 2026-06-06 — US-005 Multi-select diary entries -> create custom meal (DONE, main session)
Changes:
- FoodEntryRow: added selectionMode/isSelected/onToggleSelect/onLongPress. Row pointerInput detectTapGestures: long-press (when !selectionMode) enters selection; tap (when selectionMode) toggles. Leading Checkbox replaces drag handle in selection mode; edit/delete actions hidden in selection mode.
- ReorderableEntries + FoodDiarySection: thread selectionMode/selectedIds/onToggleSelect/onLongPress through.
- MacroDailyCounterDetail: selectionMode + selectedIds (mutableStateListOf<Long>, preserves selection order) + namingSelection state; exitSelection lambda. Long-press enters mode + selects. Bottom Surface action bar (Cancel | "N selected" | Save as meal, disabled when empty). Naming Dialog -> builds items from selectedIds order via FoodDiaryEntry.toCustomMealItem, customMealStateHolder.create, exit + snackbar. Source entries untouched.
Gates: Android + iOS BUILD SUCCESSFUL.
Learnings:
- detectTapGestures(onLongPress, onTap) on the row for selection coexists with the drag handle's own detectDragGesturesAfterLongPress (handle hidden in selection mode, so no gesture conflict).
- mutableStateListOf<Long> for selection preserves insertion order -> custom meal item sortOrder = selection order via mapIndexedNotNull over selectedIds.
- Made FoodDiaryEntry.toCustomMealItem internal (was private) in CustomMealManagerSheet.kt so MacroDailyCounterDetail reuses it (same ui.dashboard package).
