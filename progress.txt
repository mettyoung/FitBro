# Ralph Progress Log — rolling-date-range
Started: 2026-06-18

## Codebase Patterns
- **Gradle env**: JAVA_HOME=/Library/Java/JavaVirtualMachines/jdk-21.jdk/Contents/Home; run UNSANDBOXED. Typecheck = `:composeApp:compileDebugKotlinAndroid` + `:composeApp:compileKotlinIosSimulatorArm64`. No browser test.
- **UserSettingsDataSource**: `data/cache/UserSettingsDataSource.kt` (interface) + `UserSettingsDataSourceImpl.kt` (impl). Add new key constant + getter/setter alongside existing macro goal keys. Impl uses `settings.string(key, default)` pattern.
- **todayString() / minusDays**: `util/DateUtil.kt` — `todayString()` returns "YYYY-MM-DD". `String.minusDays(n)` or `String.plusDays(n)` extension also in DateUtil — use to compute default start date.
- **App.kt initialDateRange**: `val initialDateRange = DateRange(startDate = ..., endDate = todayString())` constructed before `DashboardStateHolder` is created.
- **DateRange picker callback**: `onDateRangeSelected` lambda is passed from `App.kt`/`DashboardWithTabs` into `DashboardContent`. Override end date to `todayString()` here and persist start date.
- **DashboardStateHolder.setDateRange**: `data/DashboardStateHolder.kt:146` — called when user picks a new range. Can add persistence call here if `userSettingsDataSource` is available in StateHolder; otherwise do it in the lambda before calling setDateRange.
- **CalorieBalanceChart**: `ui/dashboard/CalorieBalanceChart.kt:70` — `balances.takeLast(7)` — DELETE this line; chart already normalises bar heights to window max so all entries render correctly.
- **"Total Week" string**: `DashboardScreen.kt:523` — hardcoded `Text("Total Week"...)` — rename to `Text("Total"...)`.
